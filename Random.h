#pragma once
#include "linalg.h"

namespace CNetworks {
    //todo
    /*    class Random {
            using Generator = Eigen::Rand::Vmt19937_64;

        public:
            Random(int seed) : generator_(seed) {}
            Matrix uniformMatrix(Index rows, Index cols, double a, double b) {}
            Vector uniformMatrix(Index rows, double a, double b) {}
            Eigen::PermutationMatrix<Eigen::Dynamic> permutation(Index size) {}

        private:
            static constexpr int k_default_seed_ = 42;
            Generator generator_{k_default_seed_};
        };*/
} // namespace CNetworks
